# Copy to .env and fill in — .env is git-ignored.

# MongoDB Atlas
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/?appName=Cluster0

# Azure (only needed if running local ADLS tests)
AZURE_STORAGE_ACCOUNT=pipelinexlake
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
